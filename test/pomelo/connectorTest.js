'use strict';
const mqtt = require('mqtt');
const _ = require('underscore');
const should = require('should');

describe('pomelo connector use case test', () => {
    describe('member handler', () => {
        context('member entry', () => {
            let client;
            it('member entry is invalid if auth fail', done => {
                done();
            });
            after(done => {
                done();
            });
        });
    });
});